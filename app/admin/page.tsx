export default function AdminIndexPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4">Admin Dashboard</h1>
      <p className="text-gray-600">You are inside the Admin group (Header + Sidebar applied).</p>
    </div>
  );
}
